version: "3.9"
services:
  api:
    build: .
    image: bolcd-api:latest
    ports:
      - "8080:8080"
    environment:
      - BOLCD_API_KEYS=
    volumes:
      - ./configs:/app/configs:ro
      - ./data:/app/data:ro
      - ./reports:/app/reports
