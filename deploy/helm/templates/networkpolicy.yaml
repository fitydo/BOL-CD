{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "bolcd.fullname" . }}-np
  labels:
    app: {{ include "bolcd.name" . }}
spec:
  podSelector:
    matchLabels:
      app: {{ include "bolcd.name" . }}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
      {{- if .Values.networkPolicy.ingressFromCIDRs }}
      - ipBlock:
          cidr: 0.0.0.0/0
          except:
          {{- range .Values.networkPolicy.ingressFromCIDRs }}
            - {{ . | quote }}
          {{- end }}
      {{- end }}
      - namespaceSelector: {}
      - podSelector: {}
      ports:
        - protocol: TCP
          port: {{ .Values.service.port }}
  egress:
    # Allow egress to same-namespace bolcd Service (port service.port)
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: {{ .Release.Namespace }}
          podSelector:
            matchLabels:
              app: {{ include "bolcd.name" . }}
      ports:
        - protocol: TCP
          port: {{ .Values.service.port }}

    # Allow DNS to kube-dns (TCP/UDP 53)
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
          podSelector:
            matchLabels:
              k8s-app: kube-dns
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53

    # Allow external HTTP/HTTPS egress (e.g., cloud APIs)
    - to:
        - ipBlock:
            cidr: 0.0.0.0/0
      ports:
        - protocol: TCP
          port: 443
        - protocol: TCP
          port: 80
{{- end }}


